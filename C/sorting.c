#include <stdlib.h>







int main()
{
}





void merge_sort(int* array, int low, int high)
{
	if (low < high)
	{
		int middle = (high - low) / 2;
		merge_sort(int* array, low, middle - 1);
		merge_sort(int* array, middle, high);
		merge_sort(int* array, 
	}
	
}
